# Handoffs

This directory is intentionally empty of real tenure records.

A finalized handoff belongs here only for an explicitly authorized **true succession**. Same-incumbent chat recovery does not create a new handoff. This checked-in example contains no real predecessor/successor history.
