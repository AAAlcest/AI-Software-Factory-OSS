# Current cursor — unconfigured starter

No incumbent or project is assigned. No predecessor handoff exists.
Read `instance.json`, `authority.json` and `state.json`. Configure only inside
your own authorized instance; never place real operational records in this
distribution. This template must remain BLOCKED until explicitly configured.
