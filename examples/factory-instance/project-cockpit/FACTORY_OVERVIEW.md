# Factory overview

> DERIVED COCKPIT SNAPSHOT — repository records remain canonical.

Sources: `factory/FACTORY_STATE.md`, `factory/OPEN_LOOPS.md`, `registers/projects.json`, `registers/roles.json`.

## Current picture

- `project:atlas` — ACTIVE; synthetic review coordination is open.
- `project:beacon` — BLOCKED; Project Primary/assignment is unresolved.
- `role:management` — coordinates portfolio/product work.
- `role:governance` — checks consistency/public-safety boundaries.
- `role:runtime` — planning-only infrastructure role in this example.
- `role:portfolio-console` — visibility/control-tower role; no project execution authority.

## Human gates

Publication, credentials, production/destructive changes and external-company actions remain Human-gated or outside this synthetic example.

Next safe action: keep Beacon blocked; for Atlas, verify the current candidate before routing review work.
