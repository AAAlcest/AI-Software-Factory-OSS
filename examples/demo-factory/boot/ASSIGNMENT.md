# Synthetic assignment — Atlas

FICTIONAL FIXTURE, NOT REAL AUTHORITY. Demo Human assigns the fictional
`demo-primary-1` incumbent to `project-primary` for Project Atlas. The only
declared action is `DOCS_CHANGE` in `project:atlas`. This first appointment has
no predecessor. The fixture may be inspected but cannot authorize real work,
publication, license selection, deployment, destructive action or credentials.

No real conversation, organization, decision, timestamp, commit or review is
represented by this record. The assignment itself is input, not test evidence.
